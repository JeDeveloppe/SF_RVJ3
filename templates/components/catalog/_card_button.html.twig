{% if is_granted('ROLE_USER') %}

    {% if occasions is defined or occasion is defined %}
    {# si on affiche des occasions #}
        <a class="btn btn-dark col-9 mx-auto" href="{{ path('occasion', {'reference_occasion': item.reference, 'boite_slug': item.boite.slug , 'editor_slug': item.boite.editor.slug }) }}">Détails</a>
    {% else %}
        <a class="btn btn-dark col-9 mx-auto" href="{{ path('catalogue_pieces_detachees_demande', {'id': boite.id, 'slug':boite.slug , 'editorSlug': boite.editor.slug  }) }}">Voir les pièces disponibles</a>
    {% endif %}

{% else %}

    <a href="{{ path('app_login') }}" class="btn btn_juliette_bg_yellow mt-3 mb-2 col-10 mx-auto ">Merci de vous identifier</a>
    
{% endif %}