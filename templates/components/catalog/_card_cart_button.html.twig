{% if is_granted('ROLE_USER') %}

    {% if occasions is defined or occasion is defined %}


        {% if delivery is null %}

            <i class="fas fa-lightbulb text-info"></i> Pensez à renseigner votre <a href="{{ path('member_adresses') }}">adresse de livraison...</a>

        {% else %}

            {% if delivery == true %}
            
                <a class="btn btn_juliette btn_juliette_bg_dark" href="{{ path('panier_add_occasion', {'occasion_id': item.id }) }}">Ajouter au panier</a>

            {% else %}

            <i class="fas fa-lightbulb text-info"></i> Vous semblez être trop loin pour un retrait...</a>

            {% endif %}

        {% endif %}

    {% else %}
        <a class="btn btn_juliette btn_juliette_bg_yellow" href="{{ path('catalogue_pieces_detachees_demande', {'id': boite.id, 'slug':boite.slug , 'editorSlug': boite.editor.slug  }) }}">Voir les pièces disponibles</a>
    {% endif %}

{% else %}

    <a href="{{ path('app_login') }}" class="btn btn_juliette btn_juliette_bg_yellow mt-3 mb-2 ">Merci de vous identifier</a>
    
{% endif %}