{% if marquee is not null %}
    <div class="row justify-content-center align-items-center">
        {# <div class="col-12 rightCSS p-2 shadow">
            <div><i class="fa-solid fa-circle-info text-primary"></i> {{ marquee }}</div>
        </div>
    </div> #}
        <div class="col-11 col-md-9 mx-auto marquee">
            <h2 class="marquee_text"><i class="fa-solid fa-circle-info text-primary"></i> {{ marquee }}</h2>
        </div>
    </div>
{% endif %}