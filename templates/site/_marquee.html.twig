{% if marquee is not null %}
    <div class="row justify-content-center align-items-center">
        <div class="col-12 col-sm-11 col-md-9 col-lg-6 rightCSS p-2 shadow">
            <div>{{ marquee }}</div>
        </div>
    </div>
{% endif %}