{# {% if is_granted('ROLE_USER') %} #}
    <div class="col-11 mx-auto">
        <a class="btn btn-dark" href="{{ path('catalogue_pieces_detachees_articles_d_une_boite', {'id': boite.id, 'boiteSlug':boite.slug | lower , 'editorSlug': boite.editor.slug  }) }}">Voir les pièces disponibles</a>
    </div>
{# {% else %}

    <a href="{{ path('app_login') }}" class="btn btn-dark mt-3 mb-2">S'identifier</a>
    
{% endif %} #}